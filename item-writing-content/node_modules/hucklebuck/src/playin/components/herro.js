import h from 'snabbdom/h';

const herroEvent = (evt) => {
  console.log('herro me', evt);
};

const herro = (model) => {
  return h('button#herro',
    { on: { click: herroEvent }},
    `HARRRO, ${model.name}, u sexy mahn.`
  );
};

export default herro;

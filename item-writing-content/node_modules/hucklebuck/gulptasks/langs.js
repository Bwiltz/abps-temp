const gulp = require('gulp');
const plumber = require('gulp-plumber');

const langs = function(config) {
  return function() {
    return gulp
      .src([ './src/lang/**' ])
      .pipe(plumber())
      .pipe(gulp.dest(config.distpath + '/lang'));
  };
};

module.exports = langs;

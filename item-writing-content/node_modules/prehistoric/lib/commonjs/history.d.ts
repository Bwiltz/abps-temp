import { History, Location } from './types';
export declare function createHistory(): {
    push: (path: string, state?: any) => void;
    replace: (path: string, state?: any) => void;
    go: (amount: number) => Location | undefined;
    history: History;
};

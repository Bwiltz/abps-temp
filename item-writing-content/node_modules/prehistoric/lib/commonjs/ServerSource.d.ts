import { HistorySource, Location, Path, State } from './types';
export declare class ServerSource implements HistorySource {
    private locations;
    private position;
    push(path: Path, state?: State): {
        path: string;
        state: any;
        hash: string;
        queries: {};
    };
    replace(path: Path, state?: State): {
        path: string;
        state: any;
        hash: string;
        queries: {};
    };
    go(amount: number): Location;
    getCurrentLocation(): Location;
}
